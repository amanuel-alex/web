# Server-side only (SSR / Route Handlers): not exposed to the browser.
API_URL=http://127.0.0.1:5000

# Optional: if you call the API from Client Components (use sparingly; prefer API routes / SSR).
NEXT_PUBLIC_API_URL=http://127.0.0.1:5000
